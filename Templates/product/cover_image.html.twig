<div class="divider"> </div>
<div class="row">
    {% if bundle.config('Product.cover_image') %}
        <div class="col-md-10">
        {{ CRUD.Action.renderField('cover_image')|raw}}
        </div>
    {% endif %}
</div>
